@page
@using DbNetSuiteCore.Enums
@using DbNetSuiteCore.Web.Enums
@using DbNetSuiteCore.Helpers
@using DbNetSuiteCore.Models
@using Microsoft.Extensions.Primitives
@using DbNetSuiteCore.Web.Constants
@{
    string db = Request.Query["db"] == StringValues.Empty ? "Northwind2(mysql)" : string.Format(ConnectionStringTemplates.MySql, Request.Query["db"].ToString());
    var customersGrid = new GridModel(DataSourceType.MySql, db, "Customer");
    @(await new DbNetSuiteCore.GridControl(HttpContext).Render(customersGrid))
}