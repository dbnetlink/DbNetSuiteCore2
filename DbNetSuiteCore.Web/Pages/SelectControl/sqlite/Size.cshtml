@page
@using DbNetSuiteCore.Enums
@using DbNetSuiteCore.Web.Enums
@using DbNetSuiteCore.Helpers
@using DbNetSuiteCore.Models
@{
    var customerSelect = new SelectModel(DataSourceType.SQLite, "Northwind(sqlite)", "customers") {Size = 10, Caption = "Customers"};

    customerSelect.Columns = new List<SelectColumn>
    {
        new SelectColumn("CustomerID"),
        new SelectColumn("coalesce(CompanyName,'') || ', ' || coalesce(Address,'') || ', ' || coalesce(City,'')")
    };

    @(await new DbNetSuiteCore.Control(HttpContext).Render(customerSelect))
}